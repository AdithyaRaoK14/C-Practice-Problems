#include <stdio.h>
#include "day2.12.h"

int main() {
    long long b = 1101;
    int d = 13;
    printf("Binary to Decimal: %d\n", binaryToDecimal(b));
    printf("Decimal to Binary: %lld\n", decimalToBinary(d));
    return 0;
}